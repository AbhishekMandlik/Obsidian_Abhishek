> R² measures how well the model explains variance in the target variable.

Why is Logistic Regression called Regression?
Because:
```
It predicts a continuous score first
```
and then converts it into probability using sigmoid.
Historically it evolved from regression methods.

>Naive Bayes applies Bayes' theorem and assumes features are conditionally independent given the class label.


SVM:
Distance between classes, It chooses
```
Maximum Margin Hyperplane
```
x x x

x ← support vector

-----------

o ← support vector

o o o


```
Linear Regression
→ Predict continuous values

MSE
→ Average squared error

R²
→ Variance explained

Logistic Regression
→ Binary classification

Sigmoid
→ Converts score to probability

Decision Boundary
→ Class separator

Naive Bayes
→ Bayes theorem

Naive
→ Assumes feature independence

SVM
→ Maximum margin classifier

Hyperplane
→ Decision boundary

Margin
→ Distance from boundary

Kernel
→ Handles non-linear data
```