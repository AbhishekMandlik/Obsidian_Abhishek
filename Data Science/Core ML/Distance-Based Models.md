KNN
Compute distance to all training samples.
Pick nearest K neighbours
Vote.

> When K is too small, KNN becomes sensitive to noise and tends to overfit.
>  When K is too large, KNN over smooths decision boundaries and tends to under fit.

> KNN is a lazy learner because it does not build an explicit model during training and performs computation only when predictions are requested.


