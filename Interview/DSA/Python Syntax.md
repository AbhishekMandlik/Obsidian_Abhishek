### To check if a key is present in map
```
mp={}
if second not in mp:
	mp[second]="Abhishek"
```

### To sort the contents of a map:
```
for key in sorted(mp.keys()):
    lst.append(mp[key])
```

###